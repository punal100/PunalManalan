---
phase: 02-visual-identity-and-responsive-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - app/portfolio-data.ts
  - public/profile.jpeg
autonomous: true
requirements:
  - UI-02
  - PRES-01
must_haves:
  truths:
    - Visitor sees a darker, more polished hero with clear identity and readable hierarchy.
    - Visitor can spot public profile links and entry CTAs in the top fold without drilling into later sections.
    - About and Experience no longer waste desktop width on narrow left columns with large empty gutters.
    - Long-form content remains readable across desktop and mobile breakpoints.
  artifacts:
    - path: app/page.tsx
      provides: Refined hero, About, and Experience layout with denser full-width content surfaces.
      min_lines: 350
    - path: app/portfolio-data.ts
      provides: Shared identity links with the canonical LinkedIn profile URL used by the hero profile surfaces.
      min_lines: 300
    - path: public/profile.jpeg
      provides: Real profile image rendered in the top navigation identity.
  key_links:
    - from: app/page.tsx
      to: public/profile.jpeg
      via: top navigation `Image` component
      pattern: /profile.jpeg
    - from: app/page.tsx
      to: app/portfolio-data.ts
      via: hero, About, and Experience rendering from shared portfolio data
      pattern: portfolioData
---

<objective>
Establish the polished top fold and full-width section rhythm for the portfolio.

Purpose: Remove the visibly wasted desktop space and make the first screen feel more personal and deliberate before expanding the featured-work system.
Output: A denser `app/page.tsx` layout plus a local profile asset used in the navigation identity.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-identity-and-responsive-experience/02-CONTEXT.md
@app/page.tsx
@app/portfolio-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish hero identity and top-fold content density</name>
  <files>app/page.tsx, app/portfolio-data.ts, public/profile.jpeg</files>
  <action>Replace the generic initials badge with the real profile image from `public/profile.jpeg`, keep the hero CTAs prominent, move the public LinkedIn/GitHub profile treatments into the hero so first-view identity and outbound paths are visible without relying on the About section, and align the shared LinkedIn URL in `app/portfolio-data.ts` to the current canonical public profile path. Tighten the snapshot/current-focus composition without breaking the dark aesthetic or existing CTA affordances.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The top fold feels personal and actionable, with the real profile image and public profile links visible in the hero.</done>
</task>

<task type="auto">
  <name>Task 2: Remove desktop gutter waste from long-form sections</name>
  <files>app/page.tsx</files>
  <action>Stack the About and Experience headers above their content instead of keeping narrow split desktop columns that leave large empty areas. Let cards and text blocks use the available width while preserving the existing dark card surfaces, typography hierarchy, and semantic section structure.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>About and Experience use the full section width and no longer show obvious empty left gutters on desktop layouts.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                       |
| --------------------------------------- | ----------------------------------------------------------------- |
| local public asset -> rendered identity | The profile image becomes part of the public first-view interface |
| content module -> rendered markup       | Shared resume-backed data continues to drive visible page content |

## STRIDE Threat Register

| Threat ID | Category | Component                             | Disposition | Mitigation Plan                                                                                                                          |
| --------- | -------- | ------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-01   | T        | `public/profile.jpeg` asset ingestion | mitigate    | Use only the provided local profile image and serve it as a static public asset; do not introduce remote runtime avatar fetching.        |
| T-02-02   | I        | public hero/profile surface           | accept      | The profile image and public links are intentionally public portfolio identity elements.                                                 |
| T-02-03   | R        | layout/content fidelity               | mitigate    | Keep hero, About, and Experience rendering sourced from `app/portfolio-data.ts` so layout changes do not silently change authored facts. |

</threat_model>

<verification>
- `npm run build`
- Confirm the nav renders `/profile.jpeg` instead of the initials badge.
- Confirm About and Experience headers no longer reserve a mostly empty left column on desktop.
</verification>

<success_criteria>

- The hero presents a real profile identity and immediate outbound/public paths.
- About and Experience read as full-width sections instead of split layouts with wasted space.
- The page keeps its dark visual hierarchy while improving density and readability.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-visual-identity-and-responsive-experience/02-01-SUMMARY.md`
</output>
